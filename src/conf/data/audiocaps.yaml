# @package data

# Common params
root: "${path.data}"
bsize: 512
n_workers: ${slurm.cpus_per_task}
pin_memory: true
verbose: ${verbose}

# Other params
subsets: null
with_tags: true
download: false
audiocaps_caps_fix_fpath: null
