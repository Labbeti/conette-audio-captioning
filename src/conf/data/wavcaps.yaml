# @package data

# Common params
root: "${path.data}"
bsize: 512
n_workers: ${slurm.cpus_per_task}
pin_memory: true
verbose: ${verbose}

# Other params
subsets: [as,bbc,sb]
download: false
hf_cache_dir: null
