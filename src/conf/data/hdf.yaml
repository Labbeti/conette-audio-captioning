# @package data

# Common params
root: "${path.data}"
bsize: 512
n_workers: ${slurm.cpus_per_task}
pin_memory: true
verbose: ${verbose}

# Other params
name: ???
subsets: ???
hdf_suffix: ???
include_columns: null
