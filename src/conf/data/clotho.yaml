# @package data

# Common params
root: "${path.data}"
bsize: 512
n_workers: ${slurm.cpus_per_task}
pin_memory: true
verbose: ${verbose}

# Other params
subsets: null
version: "v2.1"
download: false
clean_archives: false
